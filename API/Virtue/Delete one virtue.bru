meta {
  name: Delete one virtue [LEGACY]
  type: http
  seq: 4
}

delete {
  url: localhost:8000/api/virtue/:virtueIdentifier
  body: json
  auth: none
}

docs {
  ⚠️  LEGACY — Backend Express (backend/) désactivé.
  Ces données sont maintenant stockées dans Supabase et gérées via l'UI MJ (Nuxt).
  Aucun endpoint HTTP public équivalent dans la nouvelle architecture.
}
