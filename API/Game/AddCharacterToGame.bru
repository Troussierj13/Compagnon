meta {
  name: AddCharacterToGame [LEGACY]
  type: http
  seq: 3
}

get {
  url: http://localhost:8000/api/game/:gameId/:characterId
  body: none
  auth: none
}

docs {
  ⚠️  LEGACY — Backend Express (backend/) désactivé.

  Équivalent dans la nouvelle architecture Nuxt :
  → POST {{baseUrl}}/api/session/join  avec character_id dans le body
  (voir dossier Session/)
}
